#########################################################################
# ShopJoy application configuration
#
# This file contains database connection settings, connection pool limits
# and basic application metadata. Replace placeholder values as needed.
#########################################################################

# PostgreSQL JDBC URL (host, port, database)
# Example: jdbc:postgresql://localhost:5432/shopjoy
db.url=jdbc:postgresql://localhost:5432/shopjoy_db

# Database credentials
db.username=postgres
db.password=Final@2025

# JDBC Driver class name
db.driver=org.postgresql.Driver

# Connection pool settings (used by the application's simple pool)
# Maximum number of connections allowed in the pool
pool.max.size=10
# Minimum number of idle connections to keep available
pool.min.idle=2

# Application metadata
app.name=ShopJoy
app.version=1.0

